real_server <%= name %> <%= port %> {
  weight <%= weight %>
<% case check -%>
<% when 'SMTP' -%>
  SMTP_CHECK {
    connect_timeout <%= connect_timeout %>
    helo_name <%= helo_name %>
  }
<% else -%>
<% raise "unknown check #{check}" %>
<% end -%>
}
